[package]
name = "context-guardian"
version = "0.2.1"
edition = "2021"

[dependencies]
serde_json = "1"
ureq = { version = "2", features = ["json"] }
rusqlite = { version = "0.32", features = ["bundled"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
